{
  /* 包含指定文件路径的数组列表，编译器在编译时只会编译其中的文件. 如果不指定，则取决于有没有设置 include 选项，
  如果没有 include 选项，则默认会编译根目录以及所有子目录中的文件 */
  "files": [],
  // 一个对象数组，指定要引用的项目
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
